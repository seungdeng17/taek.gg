const initialState = {
    summonerInfoData: null,
};

const summoner = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default summoner;
